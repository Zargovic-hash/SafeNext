import Button from "./Button";
import Card, { CardHeader, CardTitle, CardContent } from "./Card";
import Input from "./Input";
import Label from "./Label";
import LoadingSpinner from "./LoadingSpinner";
import StatusBadge from "./StatusBadge";
import Badge from "./Badge";
import Select from "./Select";


// ✅ Export individuel
export {
    Button,
  Card,
  CardHeader,
  CardTitle,
  CardContent,
  Input,
  Label,
  LoadingSpinner,
  StatusBadge,
  Badge,
  Select,
};

// ✅ Export par défaut (pratique si tu veux tout importer d’un coup)
export default {
  Button,
  Card,
  CardHeader,
  CardTitle,
  CardContent,
  Input,
  Label,
  LoadingSpinner,
  StatusBadge,
  Badge,
  Select,
};
